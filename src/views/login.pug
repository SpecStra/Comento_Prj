extends base/base

block content
    form(method="POST")
        input(name="username" placeholder="ID" type="text" value="asas" required)
        input(name="password" placeholder="Password" type="password" value="123" required)
        input(type="submit", value="Save")
    div.oath_box
        a(href="/join") Sign On